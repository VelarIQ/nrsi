# Default Locust settings for platform-api load tests.
# Usage: from this directory, run: locust -f locustfile.py
# Or: locust -f locustfile.py --config locust.conf

host = http://localhost:8080
users = 100
spawn-rate = 10
run-time = 5m
